import React from 'react';
import './../App.css';

function Homepage() {
    return(
        <div id="homepage">
            <h1>Bem-vindx ao GitHub Viewer</h1>
            <p>Funciona de forma similar como no próprio GitHub.</p>
            <p>Insira um nickname válido no campo de busca e veja a magia acontecer :)</p>
        </div>
    );
}

export default Homepage;